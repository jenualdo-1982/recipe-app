version: "1"
meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: false  # Временно отключите кэш для чистого запуска
run:
  persistenceMount: /data
  command: uvicorn back.main:app --host 0.0.0.0 --port 80
