version: "1"
meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  persistenceMount: /data
  # Указываем uvicorn зайти в папку back и запустить main.py
  command: uvicorn back.main:app --host 0.0.0.0 --port 80
