version: null
meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: false
run:
  persistenceMount: /data
  command: uvicorn back.main:app --host 0.0.0.0 --port 80
  containerPort: 80
serviceType: compute
